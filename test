/**
 * Вычисление суммы
 * @param a
 * @param b
 * @returns {*}
 */
function summ(a,b) {
    var total = a + b;
    return total;
}

/**
 * Функция делает "очень интересные" вещи
 * @returns {boolean}
 */
function someUniqFn() {
    return true;
}